#Q. Generting the randoom numbers for polyhedral(more or less than 6-sides)-dice

import random

# Using the randint function from the random module to generate a random whole integer
# between 1 and 20 (inclusive). This simulates rolling a polyhedral dice.
number = random.randint(1,20)


print(number)