import math

radius = float(input("Enter the radius of a circle: "))

circumference = 2 * math.pi * radius

print(f"The cicumference is : {circumference}")
print(f"The circumference is: {round(circumference,2)}")